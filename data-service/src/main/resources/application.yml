server:
  port:8080
spring:
  datasource:
    url: jdbc:hsqldb:mem:customersdb
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always